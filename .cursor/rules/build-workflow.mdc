---
description: 按关键词选择快速验证、正式发布或线上排查，禁止小改动走完整打包
alwaysApply: true
---

# 构建模式

改代码前先读 `PROJECT_BUILD_RULES.md` 和 `PROJECT_MAP.md`。只改当前任务那一个项目。别的项目有问题只写「发现，不处理」。

用户没点名项目时，默认只处理本仓库 LiveAssistant / StartLiveSystem。

- 启动软件、跑一下、测试一下、验证一下：快速验证。C# 用 `dotnet build`，Go 用已有 `go build`，Node 用 dev/start。禁止 `dotnet publish`、`electron-builder`、全量测试、E2E、上传更新。
- 编译发更新、发布版本、上传更新、发正式版、打包：才跑完整测试和正式打包。
- 线上有问题、崩了、用户打不开、日志分析：不重新编译。先读日志、堆栈和版本，再最小修复。

不要删除 `bin`、`obj`、`node_modules`、Go 缓存。不要 `git clean`、`git reset`。除非用户当次明确要求。

快速验证的回复只写三行：修改、验证、结果（通过/失败）。不要贴大段测试日志。

发布回复写：修改文件、测试结果、构建产物、版本、commit、更新包、git 状态。
