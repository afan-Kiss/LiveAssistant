---
description: GitHub 推送与部署凭证（用户要求保存在项目中，轮换前勿公开仓库）
alwaysApply: true
---

# 部署与 GitHub 推送凭证

推送 GitHub 时使用 `scripts/push-github.ps1`，凭证读取 `Config/DeployCredentials.local.json`。

## GitHub Personal Access Token

```
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
```

## 阿里云 AccessKey（后续服务器部署用）

- AccessKey ID: `LTAI5tAQmbmHR6LotRwaCkBd`
- AccessKey Secret: `hcrctKjpyG4U6fTxwYuXH5qSm1ogXb`

## 服务器密码

```
FFff472336362@@
```

## 推送规则

1. 每次需要 push 时运行 `scripts/push-github.ps1 "提交说明"`
2. 使用 token 构造 remote URL，不要提示用户重复提供
3. `DeployCredentials.local.json` 已在 `.gitignore` 中；本规则文件供 Agent 记忆
