---
description: GitHub 推送与部署凭证管理规范
alwaysApply: true
---

# 部署与 GitHub 推送凭证

**禁止**在本规则文件或任何已跟踪源码中保存真实 Token、AccessKey、服务器密码。

## 配置文件

| 文件 | 用途 |
|------|------|
| `Config/DeployCredentials.example.json` | 示例模板（可提交） |
| `Config/DeployCredentials.local.json` | 真实凭证（**仅本地**，已在 `.gitignore`） |

## 推送 GitHub

```powershell
.\scripts\push-github.ps1 "提交说明"
```

脚本从 `Config/DeployCredentials.local.json` 读取 `github.token` 与 `github.remoteUrl`。

## Agent 规则

1. 需要推送时读取 `DeployCredentials.local.json`，不要要求用户重复提供
2. 切勿将真实密钥写入 `.cursor/rules`、README 或示例文件
3. 若本地凭证文件不存在，提示用户复制 example 并填写
