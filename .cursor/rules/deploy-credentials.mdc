---
description: GitHub 推送与部署凭证管理规范
alwaysApply: true
---

# 部署与 GitHub 推送凭证

## 已配置（勿再向用户索要）

GitHub PAT 与部署密钥已写入 **`Config/DeployCredentials.local.json`**（本地，已在 `.gitignore`）。

推送 GitHub 时 **必须** 使用该文件中的 `github.token`，**禁止** 要求用户重复提供 Token。

## 推送命令

```powershell
.\scripts\push-github.ps1 "提交说明"
```

脚本自动从 `Config/DeployCredentials.local.json` 读取 `github.token` 与 `github.remoteUrl`，并以 `x-access-token` 方式推送。

## Agent 规则

1. 需要 `git push` 时：直接执行 `.\scripts\push-github.ps1 "message"`，或读取 `DeployCredentials.local.json` 构造 `https://x-access-token:<token>@github.com/<owner>/<repo>.git` 后推送
2. **切勿** 在对话中回显完整 Token
3. **切勿** 将 Token 写入已跟踪的源码、README 或 example 文件
4. 若 `DeployCredentials.local.json` 缺失，从 `Config/DeployCredentials.example.json` 复制并提示用户填写（正常情况下文件已存在且已填好）
