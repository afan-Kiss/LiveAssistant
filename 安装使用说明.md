# 24小时直播助手 · 安装使用说明

面向日常使用：装什么、开什么、怎么登录、怎么点歌、出问题怎么查。

---

## 1. 系统由哪几部分组成

本系统不是单个 exe 就能完成全部能力，需要 **3 个本机程序** 配合：

| 程序 | 作用 | 本机端口 |
|------|------|----------|
| **LiveAssistant.exe**（24小时直播助手） | 主程序：弹幕展示、队列、播放、自动回复、管理后台 | 管理页 `5088` |
| **抖音直播弹幕助手.exe** | 抖音 API 助手：监控弹幕、发弹幕、送礼、@回复、禁言 | `4723` |
| **酷狗api_v1.5.exe** | 酷狗 API：搜歌、取播放链接 | `17888` |

正确的抖音软件是：

`E:\我的源码目录\抖音网页弹幕\build\抖音直播弹幕助手.exe`

打开后自带 HTTP API（监控弹幕、发弹幕等）。**不要**用 24 小时无人直播，也**不要**把 Wails 福袋版 `douyin-danmaku.exe` 当主用。

关系简述：

```text
观众发弹幕「点歌 xxx」
    → 抖音 API (4723) 采到弹幕
    → LiveAssistant 识别点歌、入队
    → 酷狗 API (17888) 搜歌 / 取直链
    → LiveAssistant 用 NAudio 播放声音
```

主程序 **不会** 自己登录抖音或酷狗；登录都在对应 sidecar 软件里完成。

---

## 2. 软件路径（本机已部署）

请按下面路径找程序（资源管理器地址栏可直接粘贴）：

| 软件 | 完整路径 |
|------|----------|
| 点歌主程序（单文件包） | `E:\我的源码目录\抖音弹幕点歌系统\publish\LiveAssistant-one\LiveAssistant.exe` |
| 抖音弹幕 API | `E:\我的源码目录\抖音网页弹幕\build\抖音直播弹幕助手.exe` |
| 酷狗 API | `E:\我的源码目录\酷狗协议\KgDesktop\build\bin\酷狗api_v1.5.exe` |

源码工程配置里对应项（`LiveAssistant\Config\appsettings.json`）：

- `douyin.douyinExePath` → 上面抖音路径  
- `kugou.kugouExePath` → 上面酷狗路径  
- `douyin.baseUrl` → `http://127.0.0.1:4723`  
- `kugou.baseUrl` → `http://127.0.0.1:17888`

也可以把三个程序放进 **同一个文件夹**（推荐）：

```text
某文件夹\
  LiveAssistant.exe
  抖音直播弹幕助手.exe        ← 抖音 API 助手（监控/发弹幕）
  启动抖音API.cmd
  酷狗api_v1.5.exe
  kgapijs\                    ← 必须一起拷，否则取链会失败
```

主程序会 **优先用同目录里的 exe**。不要拷福袋/无人直播软件进来。

---

## 3. 安装前准备

### 3.1 系统要求

- Windows 10 / 11（x64）
- 可联网（抖音登录、酷狗取链、可选云端后台）
- 本机音箱/声卡正常（播放走本机默认播放设备）

### 3.2 建议安装

| 依赖 | 用途 | 说明 |
|------|------|------|
| **Node.js（LTS）** | 酷狗完整取链 | 酷狗api 会拉起旁边的 `kgapijs/`；未装 Node 时完整音可能失败 |
| **Chrome / Edge** | 抖音扫码登录 | 抖音 sidecar 用浏览器抓 Cookie |

酷狗目录旁应有：

- `酷狗api_v1.5.exe`
- `kgapijs\`（协议组件，勿删）

### 3.3 首次拷贝到新电脑（可选）

至少拷贝这三块：

1. `publish\LiveAssistant-one\`（或整份 `LiveAssistant` 发布目录）  
2. `E:\我的源码目录\抖音网页弹幕\build\抖音直播弹幕助手.exe`（及其旁 `data\`）  
3. 整个 `酷狗协议\KgDesktop\build\bin\`（含 `kgapijs`、`data`）

然后在新电脑上改 `appsettings.json`（或运行后 `data\appsettings.json`）里的两个 exe 路径。

运行时配置还可能出现在：

`%LOCALAPPDATA%\LiveAssistant\data\appsettings.json`

---

## 4. 首次使用：登录（只做一次，失效再做）

### 4.1 登录抖音（弹幕 / 互动）

1. 打开 `抖音直播弹幕助手.exe`  
2. 在软件里扫码登录（监控弹幕、发弹幕都在这里）  
3. 看到已登录后，再开点歌主程序  

Cookie 在 exe 旁 `data\cookies.json`。不要同时开两份抖音助手，以免抢 `4723` 端口。

### 4.2 登录酷狗（搜歌 / 播放）

1. 双击打开：`酷狗api_v1.5.exe`  
2. 点 **二维码登录**，用手机酷狗 App 扫码  
3. 看到已登录后再点歌  

**重要：**

- 若提示「旧版扫码残留 / 登录态无效」：在酷狗api 里 **退出 → 重新扫码**  
- 旧登录态往往只能拿到 **试听**（约 1 分钟），完整歌曲取链会失败  
- VIP 过期也会影响完整音质，以酷狗账号实际权益为准  

### 4.3 启动点歌主程序

1. 打开 `LiveAssistant.exe`  
2. 看主窗口状态栏：  
   - **抖音** = 绿色「在线」  
   - **酷狗** = 绿色「在线」  
3. 填写 **直播间短号**（webRid），点 **连接**  

主程序带看门狗：若 sidecar 没开且路径正确，会尝试自动拉起：

- 抖音：`抖音直播弹幕助手.exe`  
- 酷狗：`酷狗api_v1.5.exe`  

路径不对时，系统消息会提示「服务未配置或文件不存在」，需手动打开或改路径。

---

## 5. 日常开播流程（推荐顺序）

1. （可选先开）`酷狗api_v1.5.exe` → 确认已登录  
2. 打开 `LiveAssistant.exe`（或先开 `抖音直播弹幕助手.exe`）  
3. 确认抖音 / 酷狗均为 **在线**  
4. 输入直播间短号 → **连接**  
5. 观众发弹幕：`点歌 泡沫` 或 `点歌:泡沫`  
6. 看「点歌队列」入队，本机开始播放  

播放模式（主界面可选）：

- **点歌+随机补位**（默认）：有点歌播点歌，空闲用随机歌单补位  
- **点歌优先**  
- **随机播放**  

音量、随机歌单等可在配置里改（见第 7 节）。

---

## 6. 管理后台

### 6.1 本机后台

主程序运行后可访问：

- `http://127.0.0.1:5088/diangexitong/`

### 6.2 云端后台（已配置隧道时）

电脑开着 LiveAssistant 且云端隧道连通时：

- `https://xiangyuzhubao.xyz/diangexitong/`

默认账号（可在配置 `admin.accounts` 修改）：

| 用户名 | 密码 |
|--------|------|
| fanfan | fanfan9724 |
| laolang | qwe123 |

说明：

- 云端可登录看状态；客户端没运行时页面会显示 **没运行**  
- 点歌 / 播放仍依赖 **直播电脑** 上的三个程序  

---

## 7. 常用配置说明

配置文件：

- 工程默认：`LiveAssistant\Config\appsettings.json`  
- 单文件运行后也可能用：`%LOCALAPPDATA%\LiveAssistant\data\appsettings.json`  

| 配置项 | 含义 |
|--------|------|
| `douyin.webRid` | 默认直播间短号（也可在界面填） |
| `douyin.douyinExePath` | 抖音 exe 路径 |
| `kugou.kugouExePath` | 酷狗 exe 路径 |
| `playback.volume` | 音量 0–100 |
| `playback.autoSkipOnError` | 播放失败是否自动跳过 |
| `randomPlaylist.items` | 随机补位关键词列表 |
| `admin.accounts` | 管理后台账号 |
| `adminTunnel.*` | 云端反向隧道（一般勿改） |

回复话术：`Config\ReplyTemplates.json`。

---

## 8. 故障排查

### 8.1 主界面「抖音」红色离线

1. 打开 `抖音直播弹幕助手.exe` 并登录  
2. 浏览器访问：`http://127.0.0.1:4723/api/health`  
3. 确认配置/同目录指向 `抖音直播弹幕助手.exe`  
4. 不要同时开福袋版或第二份助手（端口会被占）  

### 8.2 主界面「酷狗」红色离线

1. 打开 `酷狗api_v1.5.exe`  
2. 访问：`http://127.0.0.1:17888/health`  
3. 同目录必须带 `kgapijs\`，且文件名是 `酷狗api_v1.5.exe`  

### 8.3 提示「播放失败，已自动跳过《》」或空歌名

1. 酷狗是否 **在线**、是否已 **退出后重新扫码**（旧登录态会 502）  
2. 同目录拷贝时不要只拷 exe，把 `kgapijs\` 一起拷，并安装 Node.js  
3. 听系统默认播放设备是否静音 / 选错设备  
4. 随机歌单不要全是空歌名；默认应有「泡沫 / 晴天 / 后来」

当前主程序取链：先完整音（`auto`），失败降级试听（`preview`）。连试听都没有，多半是酷狗服务未通或同目录缺 `kgapijs`。

### 8.4 有弹幕但点不了歌

1. 是否已 **连接** 正确直播间短号  
2. 弹幕格式是否为：`点歌 歌名` / `点歌:歌名`  
3. 管理后台是否开启了「暂停点歌」等应急开关  

### 8.5 日志位置

- LiveAssistant：程序旁或数据目录下的 `logs\*.log`  
- 酷狗远程日志（排障）：见酷狗api 说明；本机 `data\client_id.txt` 为客户端 ID  

主窗口底部也有 Sidecar 地址提示：  
`抖音 http://127.0.0.1:4723 | 酷狗 http://127.0.0.1:17888`

---

## 9. 一页速查

**开播最少步骤**

```text
1. 抖音直播弹幕助手.exe → 扫码登录（监控/发弹幕）
2. 酷狗api_v1.5.exe      → 扫码登录（失效就重扫）
3. LiveAssistant.exe     → 抖音/酷狗变绿 → 填短号 → 连接
4. 观众：点歌 歌名
```

**三个路径**

```text
E:\我的源码目录\抖音弹幕点歌系统\publish\LiveAssistant-one\LiveAssistant.exe
E:\我的源码目录\抖音网页弹幕\build\抖音直播弹幕助手.exe
E:\我的源码目录\酷狗协议\KgDesktop\build\bin\酷狗api_v1.5.exe
```

**两个后台**

```text
本机：http://127.0.0.1:5088/diangexitong/
云端：https://xiangyuzhubao.xyz/diangexitong/
```

---

## 10. 开发者构建（可选）

仅改主程序源码时：

```bat
cd LiveAssistant
dotnet build -c Release
dotnet run --project LiveAssistant
```

单文件发布脚本（仓库内）：`scripts\publish-single.py`。

推送相关说明见仓库根目录 `README.md`。
