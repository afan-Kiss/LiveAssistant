# 24小时直播助手 — 功能说明（供代码审查）

> 版本：2026-09-15  
> 点歌专题说明见：`点歌流程-代码审查.md`  
> 测试：`dotnet test LiveAssistant.Tests`

---

## 1. 系统架构

| 组件 | 说明 |
|------|------|
| `LiveAssistant.exe` | 主程序（WinForms + 内嵌管理后台） |
| 抖音直播弹幕助手.exe | Sidecar，弹幕/礼物/回复 API（:4723） |
| 酷狗api_v1.5.exe + kgapijs/ | Sidecar，搜歌/取链/登录/VIP（:17888） |
| 管理后台 | `http://127.0.0.1:5088/diangexitong` |

弹幕处理主链路：`DanmakuService` → `LiveAppHost.OnDanmakuReceived` → 各业务 Service。

---

## 2. 弹幕点歌

### 2.1 触发格式

支持以下格式（`SongNameParser`）：

- `点歌 泡沫`
- `点歌:泡沫` / `点歌：泡沫`
- `点歌泡沫` / `点歌双截棍`（无空格）

**不会误解析**：`点歌成功《xxx》`、`点歌失败`、`找到多首`、`是否确定点歌` 等机器人回复文案。

### 2.2 多轮对话流程

```
用户: 点歌 泡沫
  ↓ 搜索酷狗，按歌手去重，最多展示 3 位
[多个结果]
  → 回复: 找到多首《泡沫》，请回复歌手名：A、B、C
  → 用户回复歌手名（支持模糊匹配）
[单个结果则跳过选歌手]
  ↓
  → 回复确认提示（见下）
  → 用户必须回复「确定」（或：确认、是、好的、可以 等）
  ↓
  → 解析直链 → 加入播放队列 → 回复排队情况
```

**重要**：免费模式与积分模式都必须回复「确定」后才入队，之前不会播放。

- 免费模式提示：`是否确定点歌《歌名》- 歌手？回复 确定 开始点歌`
- 积分模式提示：`是否确定点歌《歌名》- 歌手？需要 N 积分，回复 确定 开始点歌`
- 入队成功：`点歌成功《歌名》\n前面还有N首`（通过 mention API @ 用户）

会话超时：**3 分钟**（`SongRequestSessionStore`）。可发送 `取消` 放弃。

### 2.3 点歌模式（管理后台 → 点歌设置）

| 模式 | 说明 |
|------|------|
| **免费点歌** | 不扣积分，受冷却/队列/等级限制 |
| **积分点歌** | 每次点歌扣 `pointsCost` 积分（默认 10） |
| **礼物解锁** | 累计积分/指定礼物解锁点歌资格 |

积分在**确认入队时**扣除（`RecordSuccessfulRequest`）。点歌前 `Evaluate` 会预检积分是否足够。

### 2.4 排队与播放

- 用户点歌进入 `_waiting` 队列，VIP/等级可插队（`AddWithPriority`）
- 随机补位曲 **不计入**「前面还有 N 首」（`GetAheadCount` 忽略 `IsRandom`）
- 点歌成功后触发 `EnsurePlaying`；若当前有曲在播/加载中则等待，不会抢播

### 2.5 防重复

- 弹幕 `msg_id` 去重（`DanmakuDeduplicator`）
- 同用户+同内容 20 秒内去重（双 feed 重复）
- 过滤本账号（抖音登录昵称）发出的弹幕
- 点歌命令 15 秒内同用户同内容去重（`SongRequestDeduper`）

---

## 3. 弹幕切歌

指令：`切歌` / `下一首` / `跳过` / `skip`

| 模式 | 是否扣积分 |
|------|-----------|
| 免费点歌 | 不扣 |
| 礼物解锁 | 不扣 |
| 积分点歌 | 扣 `skipPointsCost`（默认 20，可配置） |

- 管理员/房管切歌不扣积分
- 当前无可切歌曲时拒绝，不扣积分
- 成功后 @ 回复用户

---

## 4. 查积分

指令：`查积分` / `我的积分` / `积分查询`

回复当前积分与等级（模板 `pointsQuery`，mention @ 用户）。

---

## 5. 礼物

- 收礼记录积分、升级、点歌资格（`GiftService`）
- **礼物感谢**：收礼成功后自动 @ 感谢（模板 `giftThanks`：「感谢送出 {gift}×{count}」）
- 礼物采集由 `GiftCollectorService` 负责，软件关闭时不采集

---

## 6. 酷狗播放

### 6.1 取链顺序

完整版要求时优先 `full` → `auto`；仅未要求完整版时才允许 `preview`。

- `requireFullPlayback=true`：**不静默降级试听**，失败返回 null（可换同歌手备用 hash）
- 业务码 `502` +「旧版扫码/登录态无效」按 **会员/版权受限** 处理，**不清登录态**（与真掉线 `40101` 区分）
- 自动领 VIP 失败有时间退避，避免狂打 sidecar

### 6.2 Sidecar 启动

- 发布目录需含：`酷狗api_v1.5.exe`、`kgapijs/`、`抖音直播弹幕助手.exe`
- `SidecarBootstrap`：启动时从 `sidecars/` 或上级仓库自动补齐
- `scripts/sync-sidecars.py`：同步 sidecar；文件被占用时保留已有文件继续

---

## 7. 播放队列与切歌逻辑

- 播放命令串行执行（`PlaybackCommandQueue`）
- 用户点歌优先于随机补位
- 播放失败自动跳过并继续下一首
- 管理后台/主界面可手动跳过、暂停、上一首

### 已知曾修复的问题

| 问题 | 修复 |
|------|------|
| 提示「前面 1 首」但立刻播放点歌 | `EnsurePlaying` 在 `_nowPlaying` 存在或加载中不 advance |
| 随机曲计入排队数 | `GetAheadCount` 忽略 `IsRandom` |
| 只能试听 | auto→full 链；登录态不降级 preview |
| 点歌回复发两次 | 机器人弹幕过滤 + 内容去重 |
| 缺少酷狗 sidecar | SidecarBootstrap + sync 脚本加固 |

---

## 8. 管理后台功能

- **点歌设置**：免费/积分/礼物解锁、点歌积分、切歌积分
- **礼物积分**：自定义礼物对应积分
- **用户管理**：搜索用户、改积分/等级
- **回复模板**：点歌/查积分/切歌/礼物感谢等话术
- **播放控制**：跳过、暂停、清空队列

---

## 9. 回复模板键名

| 键 | 用途 |
|----|------|
| `songRequestChooseArtist` | 多结果选歌手 |
| `songRequestConfirm` | 免费模式确认点歌 |
| `songRequestConfirmPoints` | 积分模式确认点歌 |
| `songRequestArtistNotFound` | 歌手名未匹配 |
| `songRequestCancelled` | 用户取消 |
| `songNotFound` | 未找到歌曲 |
| `songRequestCooldown` | 冷却中 |
| `pointsQuery` | 查积分 |
| `giftThanks` | 礼物感谢 |
| `skipSongSuccess` / `skipSongSuccessPaid` | 切歌成功 |

mention 回复的文案**不含 `@`**，由抖音 mention API 负责 @。

---

## 10. 关键源文件（审查入口）

```
LiveAssistant/Services/SongRequestService.cs      # 多轮点歌主逻辑
LiveAssistant/Services/SongRequestSessionStore.cs # 用户会话状态
LiveAssistant/Services/SkipSongService.cs         # 弹幕切歌
LiveAssistant/Services/KugouService.cs            # 搜歌/取链/VIP
LiveAssistant/Services/PlaybackCommandQueue.cs    # 播放队列与 EnsurePlaying
LiveAssistant/Services/QueueService.cs            # 排队计数
LiveAssistant/Services/DanmakuService.cs          # 弹幕采集与过滤
LiveAssistant/Services/GiftService.cs             # 礼物与感谢
LiveAssistant/Utils/SongNameParser.cs             # 点歌解析
LiveAssistant/Utils/SongRequestConfirmParser.cs   # 确定/取消解析
LiveAssistant/SidecarBootstrap.cs                 # Sidecar 自动补齐
```

---

## 11. 测试覆盖

| 测试文件 | 覆盖 |
|----------|------|
| `SongRequestConfirmFlowTests` | 免费/积分模式必须确定才入队 |
| `SongRequestFlowTests` | 歌手匹配、确认解析、会话过期 |
| `SkipSongTests` | 切歌积分逻辑 |
| `KugouServiceTests` | auto/full/preview、VIP 领取 |
| `QueueAheadCountTests` | 随机曲不计入 ahead |
| `PlaybackEnsurePlayingTests` | 有曲在播时不抢播 |
| `GiftThanksTests` | 礼物 @ 感谢 |
| `PipelineParseTests` | 点歌格式与机器人文案排除 |

---

## 12. 建议审查重点（潜在风险）

1. **会话并发**：同一用户快速发多条弹幕，`_gate` 串行是否足够？
2. **歌手模糊匹配**：`ArtistNameMatcher` 部分匹配误选（多个歌手都含「李」）
3. **积分双扣**：确认时 `Evaluate` + `RecordSuccessfulRequest` 是否可能重复扣？
4. **会话与新的点歌**：发新 `点歌 xxx` 会清除旧会话，是否合理？
5. **酷狗 VIP**：侧车登录过期时积分模式点歌确认后可能解析失败
6. **机器人过滤**：仅按登录昵称过滤，换昵称后可能误处理自己的回复
7. **冷却计时**：搜索+选歌手+确认期间是否占用冷却（当前在入队时 Record）

---

## 13. 构建与发布

```bash
dotnet test LiveAssistant.Tests
python scripts/sync-sidecars.py
python scripts/publish-single.py   # 需 DeployCredentials.local.json
```

发布产物：`publish/LiveAssistant-one/LiveAssistant.exe`
