# 点歌流程说明（供 ChatGPT / 代码审查）

> 打包日期：2026-09-15  
> 范围：弹幕点歌 → 确认 → 酷狗取链 → 入队播放  
> 主入口源码见文末「审查文件清单」

---

## 0. 一句话总览

观众发 `点歌 歌名` → 主程序搜酷狗并 @ 询问是否确定 → 观众回 `确定` → 取完整播放地址 → 入播放队列 → @ 回复排队情况。

**默认必须确认后才入队**（`SongRequestPolicy.RequireConfirm = true`）。

---

## 1. 组件与数据流

```
抖音直播间弹幕
    ↓
抖音 Sidecar (:4723)  /api/live/danmaku/feed + /at/feed
    ↓
DanmakuService（拉取、msg_id 去重、过滤机器人自己）
    ↓
LiveAppHost.OnDanmakuReceived
    ↓
SongRequestService.HandleDanmakuAsync   ← 点歌主逻辑
    ↓
KugouService（搜索 / 取链） ←→ 酷狗 Sidecar (:17888)
    ↓
QueueService + PlaybackCommandQueue（排队 / 串行播放）
    ↓
ReplyQueue → 抖音 mention API（@ 用户，文案本身不含 @）
```

| 组件 | 路径 / 端口 | 职责 |
|------|-------------|------|
| 主程序 | `LiveAssistant` | WinForms + 业务编排 |
| 抖音助手 | `:4723` | 弹幕采集、发 mention |
| 酷狗 API | `:17888` | 搜歌、直链、登录/VIP |

---

## 2. 用户可见流程（状态机）

```
[无会话]
   │  弹幕匹配「点歌…」
   ▼
搜索酷狗（SearchCandidatesAsync，按歌手去重最多 3 条）
   │
   ├─ 0 首 → @「未找到」→ 结束
   ├─ 多首 → 自动选第 1 首（旧「选歌手」步已弱化：收到会话后直接进 Confirm）
   └─ ≥1 首 → 写入 SongRequestSession(Step=Confirm)
                 │
                 ▼
           @「是否确定点歌《歌》- 歌手？回复 确定 开始点歌」
                 │
     ┌───────────┼───────────┐
     │           │           │
   确定        取消        再发「点歌 xxx」
     │           │           │
     ▼           ▼           ▼
  权限+取链    清会话      清旧会话，开新点歌
  入队/@成功   @已取消
     │
     ├─ 成功 → 清会话
     └─ 取链失败 → 保留会话，ConfirmConsumed=false，可再发「确定」重试
```

### 2.1 触发文案（`SongNameParser`）

支持：`点歌 泡沫` / `点歌:泡沫` / `点歌：泡沫` / `点歌泡沫`。

**排除**机器人回复，例如：`点歌成功`、`是否确定点歌`、`播放地址获取失败`、`当前没有待确认的点歌` 等（避免自己 @ 自己再次进流程）。

### 2.2 确认 / 取消（`SongRequestConfirmParser`）

- 确定：`确定`、`确认`、`是`、`好的`、`可以` 等  
- 取消：`取消`、`算了` 等  
- 待确认期间的闲聊：**不重发**确认问句（防刷屏）

### 2.3 会话

- 存储：`SongRequestSessionStore`（按 `userId`）  
- 超时：约 **5 分钟**（以源码常量为准）  
- 用户级串行：`SongRequestUserGateRegistry`（同一用户弹幕串行处理）

### 2.4 模式与积分（确认入队时扣）

| Mode | 行为 |
|------|------|
| Free | 不扣积分 |
| Points | 确认成功入队时扣 `PointsCost` |
| GiftUnlock | 需累计积分/指定礼物解锁 |

权限预检：`SongRequestPermissionService.Evaluate` / `EvaluateSong`（冷却、黑名单、等级、积分、歌名黑名单等）。

---

## 3. 代码级调用链（确认入队）

`SongRequestService.HandleConfirmAsync`：

1. 校验是「确定」且 `ConfirmConsumed == false`  
2. `Evaluate` / `EvaluateSong`  
3. `ConfirmConsumed = true`（防双 feed 重复确认）  
4. `ResolveAndEnqueueAsync`：
   - `KugouService.ResolveCandidateAsync` / 取链  
   - `IsAcceptableForPlayback`（`RequireFullPlayback` 时拒绝试听链）  
   - `QueueService` 入队 + `RecordSuccessfulRequest`（扣积分/记冷却）  
   - `ReplyQueue.EnqueueSongRequestReply` → 「点歌成功《x》，前面还有 N 首」  
5. 成功则清会话；**取链失败则恢复 `ConfirmConsumed=false` 并保留会话**

「前面还有 N 首」：`QueueService.GetAheadCount` **忽略随机补位曲**（`IsRandom`）。

---

## 4. 弹幕去重与防刷（易踩坑）

| 层 | 位置 | 行为 |
|----|------|------|
| msg_id | `DanmakuService` | **一律去重**（含点歌/确定，避免历史回放重复处理） |
| 内容短窗 | `DanmakuService` | 同用户同内容短窗去重 |
| 机器人 | `DanmakuService` | 过滤本账号昵称发出的弹幕 |
| 点歌命令 | `SongRequestDeduper` | 同用户同点歌文案短窗 |
| 确认标记 | `ConfirmConsumed` | 同会话只消费一次确认 |
| 孤儿确定 | `TrySendOrphanConfirmHint` | 无会话时「确定」**30 秒内只提示一次**「没有待确认」 |
| CatchUp | `DanmakuService` | 启动追光标等缓冲稳定，降低把历史弹幕当新消息 |

---

## 5. 酷狗取链（与「播放地址获取失败」强相关）

配置：`Kugou.RequireFullPlayback = true`（直播场景要求完整版，不静默用试听）。

### 5.1 `GetPlayUrlCoreAsync` 策略

1. `EnsureLoginReady`  
2. 模式：完整版要求时优先 `full` → `auto`（不允许则不走 `preview`）  
3. **真掉线**（如业务码 `40101`）：清登录缓存，强制 refresh 后再试（最多约 3 轮）  
4. **伪掉线 / 会员受限**（业务码 `502` + 文案含「旧版扫码」「登录态无效」）：  
   - 判定为 `copyright_restricted`，**不清登录态**  
   - 可尝试自动领 VIP（有失败退避，避免狂打接口）  
   - 再搜同歌手同名 **备用 hash**  
5. 点歌路径同样允许同歌手备用版本（避免主 hash 仅会员可播时直接失败）

### 5.2 已知现场现象（审查时注意）

- 账号显示「超级VIP」但 `vip_end` 可能已过期（如 2026-08-07）  
- 过期后：免费曲 full 成功；会员曲 full/auto 常返回上述 502 伪会话文案；preview 仍可能成功  
- 自动领试用 VIP 可能 `51002/502` 失败 → 已做时间退避  

审查重点：`KugouService.IsPrivilegeMaskedAsStaleSession` / `IsSessionDroppedResponse` / `ResolveFreshTrackAsync`。

---

## 6. 播放侧

- `PlaybackCommandQueue`：播放命令串行  
- 点歌入队后 `EnsurePlaying`：若已有 nowPlaying / 加载中则不抢播  
- 播放前可 `ResolveFreshTrackAsync` 刷新直链；失败按策略跳过  

---

## 7. 回复模板键（`ReplyTemplates.json`）

| 键 | 用途 |
|----|------|
| `songRequestConfirm` | 免费确认问句 |
| `songRequestConfirmPoints` | 积分确认问句 |
| `songRequestChooseArtist` | 多结果选歌手（若启用） |
| `songRequestCancelled` | 取消 |
| `songNotFound` | 未找到 |
| `songResolveFailed` | 播放地址获取失败 |
| `songRequestCooldown` | 冷却 |

mention 正文**不要带 `@`**，由 API 负责 @。

---

## 8. 建议审查问题清单

1. 确认入队与扣积分是否可能双扣（`Evaluate` vs `RecordSuccessfulRequest`）  
2. 同用户并发弹幕：`_userGates` 是否覆盖所有入口  
3. 取链失败保留会话：用户连点「确定」是否应限频失败提示  
4. 备用 hash 是否可能换成「同名不同版」（翻唱/伴奏）  
5. `RequireFullPlayback` + VIP 过期时，产品上是「换免费版」还是「明确提示需 VIP」更合适  
6. CatchUp / msg_id 去重是否会误伤极短时间真实重复点歌  
7. 管理后台改 `RequireConfirm` / 模式后，热更新与迁移标记（`.migrated-require-confirm-*`）是否清晰  

---

## 9. 审查文件清单（优先读）

```
点歌流程-代码审查.md          # 本文
功能说明.md                    # 功能总览

LiveAssistant/Services/SongRequestService.cs
LiveAssistant/Services/SongRequestSessionStore.cs
LiveAssistant/Services/SongRequestPermissionService.cs
LiveAssistant/Services/DanmakuService.cs
LiveAssistant/Services/KugouService.cs
LiveAssistant/Services/KugouSongContext.cs
LiveAssistant/Services/QueueService.cs
LiveAssistant/Services/PlaybackCommandQueue.cs
LiveAssistant/Services/ReplyQueue.cs
LiveAssistant/Services/LiveAppHost.cs
LiveAssistant/Utils/SongNameParser.cs
LiveAssistant/Utils/SongRequestConfirmParser.cs
LiveAssistant/Utils/ArtistNameMatcher.cs
LiveAssistant/Models/SongRequestSession.cs
LiveAssistant/Models/SongRequestPolicy.cs
LiveAssistant/Config/AppSettings.cs
LiveAssistant/Config/ReplyTemplates.json

LiveAssistant.Tests/SongRequestConfirmFlowTests.cs
LiveAssistant.Tests/SongRequestFlowTests.cs
LiveAssistant.Tests/KugouServiceTests.cs
LiveAssistant.Tests/PipelineParseTests.cs
LiveAssistant.Tests/PlaybackStabilityTests.cs
```

### 建议本地命令

```bash
dotnet test LiveAssistant.Tests
```

---

## 10. 本包刻意排除的内容

- `publish/`、`sidecars/` 二进制与运行时 `data/`（含 Cookie、密码、隧道密钥）  
- `.git/`、`bin/`、`obj/`、日志  
- 任何 `DeployCredentials*.json`、生产 `appsettings` 实机密  

请只基于源码与本文做逻辑审查，勿假设包内含可运行 sidecar。
